<form method="post" action="{{ path('app_admin_employe_delete', {'id': employe.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir anonynimiser cette personne ?');" class="mt-5">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employe.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
