{% extends 'admin/base_admin.html.twig' %}

{% block title %}Modifier un emplacement - Admin Grand Angle{% endblock %}

{% block main %}
<div class="container-fluid py-4">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 text-white">Modifier l'emplacement</h1>
    </div>

    <div class="card bg-dark border-secondary">
        <div class="card-body">
            {{ include('admin/emplacement/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
    </div>

    <div class="mt-3">
        {{ include('admin/emplacement/_delete_form.html.twig') }}
    </div>

</div>
{% endblock %}