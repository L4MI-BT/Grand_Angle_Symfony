<!DOCTYPE html>

<title>Fonction</title>

{% block body %}
    <h1>Fonction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fonction.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ fonction.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_fonction_index') }}">back to list</a>

    <a href="{{ path('app_admin_fonction_edit', {'id': fonction.id}) }}">edit</a>

    {{ include('admin/fonction/_delete_form.html.twig') }}
{% endblock %}
