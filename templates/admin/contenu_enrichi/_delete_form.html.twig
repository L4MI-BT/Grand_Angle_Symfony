<form method="post" action="{{ path('app_admin_contenu_enrichi_delete', {'id': contenu_enrichi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contenu_enrichi.id) }}">
    <button class="btn">Delete</button>
</form>
