{% block header %}
    <nav class="nav d-flex justify-content-between">
        <h1>Grand Angle</h1>
        <div class="dropdown">
            <button class="dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                <img src="{{ asset('styles/images/drapeau_francais.svg') }}" height="15px" width="15px" alt="blabla">FR
            </button>
            {# <div class="dropdown-menu">
                    <ul>
                        {% for langue in langues %}
                            <li>
                                <a class="dropdown-item" href="{{ "public/#{langue.code}" }}">
                                    <img src="{{ asset(langue.img) }}" alt="{{ langue.nom }}" height="20px" width="20px">
                                    {{langue.nom}}
                                </a>
                            </li>
                        {% endfor %}
                    </ul>
                </div> #}
        
        </div>

    </nav>
{% endblock %}
