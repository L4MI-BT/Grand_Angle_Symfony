{% block header %}
    <nav class="nav d-flex justify-content-between">
        <h1>Grand Angle</h1>
        <div class="dropdown">
            <button class="dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                <img src="{{ asset('img/drapeau_francais.svg') }}" height="15px" width="15px" alt="error">FR
            </button>
            <div class="dropdown-menu">
                    {# <ul>
                        {% for langue in langues %}
                            <li>
                                <a class="dropdown-item" href="#">
                                    <img src="{{ asset('img/drapeau_francais.svg') }}" alt="{{ asset('img/drapeau_francais.svg') }}" height="20px" width="20px">
                                    FR
                                </a>
                            </li>
                        {% endfor %}
                    </ul> #}
                </div>
        
        </div>

    </nav>
{% endblock %}
